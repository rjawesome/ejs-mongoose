<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/index.css">
    <title>Ejs + Mongoose</title>
  </head>
  <body style="text-align: center;">
    <h1>Database Stuffs</h1>
    <% for( let i = 0; i < stuffs.length; i++ ) { %>
      <div class="card">
        <div class="card-body">
          <h2 class="card-title"><%= stuffs[i].title %></h2>
          <p class="card-text"><%= stuffs[i].description %></p>
          <form method="GET" action="edit/<%= stuffs[i]._id %>">
            <button class="btn btn-primary">Edit</button>
          </form>
          <form method="POST" action="delete/<%= stuffs[i]._id %>">
            <button class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
    <% } %> 
    <br>
    <form method="GET" action="/add">
      <button class="btn btn-success">Create new stuff</button>
    </form>
  </body>
</html>